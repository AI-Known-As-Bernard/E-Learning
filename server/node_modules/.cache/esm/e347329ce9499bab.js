let express,cors;_e94‍.w("express",[["default",["express"],function(v){express=v}]]);_e94‍.w("cors",[["default",["cors"],function(v){cors=v}]]);

const morgan = require('morgan');
require('dotenv').config()

//create express app
const app = express()


//apply middleware
app.use(cors())
app.use(express.json())
app.use(morgan("dev"))
app.use(console.log('middleware testing'))


//routes
app.get('/',(req,res) => {
    res.send("server endpoint")
})

//port
const PORT = process.env.PORT || 6000;

app.listen(PORT,() => {console.log(`Listening on ${PORT}`)})